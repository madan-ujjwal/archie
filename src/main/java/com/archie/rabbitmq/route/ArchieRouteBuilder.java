package com.archie.rabbitmq.route;

import org.apache.camel.builder.RouteBuilder;
import org.springframework.stereotype.Component;

@Component
public class ArchieRouteBuilder extends RouteBuilder {

    @Override
    public void configure() throws Exception {

        /**
         * TODO complete the route
         * auto wire queue definition and fetch the queue url
         * define from where to pick the payload
         * auto wire queue processor and provide that as the processor for the route
         */

    }
}